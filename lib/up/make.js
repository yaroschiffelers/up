/**
 * Let the user create a custom up cli shortcut
 * Instead of 'up'...
 */
'use strict'

module.exports = changeCommandName = (name, bin) => {
    if (is(name, isString) && is(bin, isObject)) {
        // todo
    }
}
